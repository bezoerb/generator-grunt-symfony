language: node_js
node_js:
- iojs
- '0.10'
- '0.12'
before_install:
- sudo apt-get update
- sudo apt-get install python-software-properties
- sudo add-apt-repository ppa:ondrej/php5 -y
- sudo apt-get update
- sudo apt-get install php5
- sudo apt-get install php5-curl
- php --version
- php -r "readfile('https://getcomposer.org/installer');" | php
- php composer.phar config --global github-oauth.github.com $COMPOSER_AUTH
- php composer.phar config --list
- gem update --system
- gem install sass --version "=3.3.7"
- npm cache clean
- npm update -g npm
- npm install -g bower grunt-cli jspm
- jspm config registries.github.auth $JSPM_AUTH
- currentfolder=${PWD##*/}
- if [ "$currentfolder" != 'generator-grunt-symfony' ]; then cd .. && eval "mv $currentfolder generator-grunt-symfony" && cd generator-grunt-symfony; fi
