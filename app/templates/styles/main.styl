<% if (useBootstrap) { %> @import "/bootstrap-stylus/bootstrap" <% } else if (useFoundation) { %>@import "/foundation/css/foundation.css";<% } %>
<% if (!useBootstrap) { %> @import "/sass-bootstrap-glyphicons/css/bootstrap-glyphicons.css"; <% } %>

.header
    height: 50px;


.yeoman-team
    max-width: 90%;
    display:block;
    margin: 20px 0 50px;
